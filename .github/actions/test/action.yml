name: "Test Pipeline Ation"
description: "Checking something..."
inputs:
  name:
    description: "A name"
    required: true
runs:
  using: "composite"
  steps:
    - name: Git-Clone
      uses: actions/checkout@v4
    - name: Git-Clone
      uses: actions/checkout@v4
      with:
        repository: jdfi-dev/iac
        path: jfdi-iac
    - name: List parent dir (recursively)
      run: ls .. -laR
      shell: bash
